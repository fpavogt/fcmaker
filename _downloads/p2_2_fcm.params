userId:                  # p2 username (will ask at prompt if left blank)
pswd:                    # p2 password (will ask at prompt if left blank)
obIds: [1848649,1848655] # List of Ob IDs to chart
bk_images: [SDSSr, None] # SkyView survey, None for default, or local FITS filename
bk_lams: [None,None]     # Lambda of the chart (SkyView will override), None for default
data_loc: fcm_data       # Rel. path to the bk images (local FITS file or SkyView images) 
plot_loc: fcm_plots      # Rel. path to store the finding charts 